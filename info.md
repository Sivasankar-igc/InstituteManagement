use the below command to install the required python libraries:
#pip install fastapi uvicorn google-generativeai python-multipart PyPDF2 scikit-learn numpy python-dotenv

create a .env file to add the below Global Variable
GEMINI_API_KEY = <your_gemini_api_key>
GEMINI_MODEL = <current gemini model ex. gemini-3-flash-preview>
